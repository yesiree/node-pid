export { create, compact, simple, toUuid };

declare function create({ uuid: string, base: number })
declare function compact()
declare function simple()
declare function toUuid(pid: string, base: number)
